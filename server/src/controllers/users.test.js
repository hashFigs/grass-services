const UserController = require('./users');


describe('61834610', () => {
  it('REGISTRATION - it should throw 400 error if email is empty string', async () => {

  });

  it('REGISTRATION - it should throw 400 error if password is empty string', async () => {
   
  });

  it('REGISTRATION - it should throw 400 error if email is already in use', async () => {
    
    
  });


});